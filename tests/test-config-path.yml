name: Test config-path input
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: push
      GITHUB_REF: refs/heads/main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up ESLint project with custom config in temp directory
        run: |
          mkdir -p /tmp/eslint-config-test/src
          # Copy fixture files
          cp "$GITHUB_WORKSPACE/actions/hardened/CatChen--eslint-suggestion-action/v4.1.27/tests/fixtures/eslint-project/package.json" /tmp/eslint-config-test/
          cp "$GITHUB_WORKSPACE/actions/hardened/CatChen--eslint-suggestion-action/v4.1.27/tests/fixtures/eslint-project/custom.eslint.config.js" /tmp/eslint-config-test/
          cp "$GITHUB_WORKSPACE/actions/hardened/CatChen--eslint-suggestion-action/v4.1.27/tests/fixtures/eslint-project/src/index.js" /tmp/eslint-config-test/src/
          # Install ESLint
          cd /tmp/eslint-config-test
          npm install --save-dev eslint@^9.0.0 @eslint/js@^9.0.0 2>&1
          echo "ESLint installed successfully"
          ls node_modules/eslint/lib/api.js || echo "eslint api.js not found"

      - name: Run ESLint suggestion action with custom config-path
        id: action
        uses: .
        with:
          github-token: ${{ github.token }}
          directory: /tmp/eslint-config-test
          targets: src/index.js
          eslint-lib-path: ./node_modules/eslint/lib/api.js
          config-path: ./custom.eslint.config.js
          request-changes: false
          fail-check: false

      - name: Assert action completed successfully with custom config
        run: |
          echo "Action completed with custom config-path without error"
          echo "PASS"
